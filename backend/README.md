## Backend for CoinAlert

#### Usage guide

###### Pre-requisites
- Python v3.9+

###### Installation
- Run the command `pip install -r requirements.txt` (If it doesn't work try again with `pip3`)

###### Execution
- Run the command `uvicorn main:app --reload`